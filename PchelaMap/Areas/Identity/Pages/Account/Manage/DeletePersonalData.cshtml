@page
@model DeletePersonalDataModel
@{
    ViewData["Title"] = "Удалить персональные данные?";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<h4>@ViewData["Title"]</h4>

<div class="alert alert-warning" role="alert">
    <p>
        <strong>Удаление персональных данных удалит Ваш аккаунт без возможности восстановления. </strong>
     </p>
    <p>
        <strong>Будет удалена информация о Ваших заданяих (в том числе, взятых другими пользователями), взятых Вами заданиях и все файлы, загруженные Вами на сервер .</strong>
    </p>
</div>

<div>
    <form id="delete-user" method="post" class="form-group">
        <div asp-validation-summary="All" class="text-danger"></div>
        @if (Model.RequirePassword)
        {
    <div class="form-group">
        <label asp-for="Input.Password"></label>
        <input asp-for="Input.Password" class="form-control" />
        <span asp-validation-for="Input.Password" class="text-danger"></span>
    </div>}
        <button class="btn btn-danger" style="margin: 2vw;" type="submit">Удалить данные и выйти из аккаунта</button>
       
        <a asp-area="Identity" asp-page="/Account/Manage/Index" class="btn btn-primary" style="margin: 2vw;" >Отмена</a>
    
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}